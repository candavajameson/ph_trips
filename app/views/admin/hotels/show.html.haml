%h1= @hotel.name
%hr

- content_for :scripts do
	= render 'shared/gmaps'

.banner-container
	%p= @hotel.address
	= image_tag (@hotel.banner_url), class: 'banner' if @hotel.banner?

.map_container
	#map.gmaps4rails_map	

	